Tuple Methods in Python
What are Tuple Methods?

Tuples are immutable, so they have very few methods compared to lists.

Python provides only two built-in tuple methods.

1️⃣ count()

Used to count how many times an element appears in a tuple.

t = (1, 2, 2, 3, 2)

print(t.count(2))

Output

3
2️⃣ index()

Used to find the index position of an element.

t = (10, 20, 30, 40)

print(t.index(30))

Output

2
