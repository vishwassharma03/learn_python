Keywords in Python:-

What are Keywords?

Keywords are reserved words in Python that have a special meaning and cannot be used as variable names, function names, or identifiers.

Python uses keywords to define the syntax and structure of the programming language.

List of Python Keywords

Some commonly used Python keywords are:

False   await   else   import   pass
None    break   except  in      raise
True    class   finally is      return
and     continue for    lambda  try
as      def      from   nonlocal while
assert  del      global not     with
async   elif     if     or      yield

Example of Keywords Usage
if True:
    print("Hello Python")
